# Memoria pendiente/confirmada

Las memorias propuestas por la IA NO se guardan aquí automáticamente.

Solo cuando pulsas **Guardar memoria** en la interfaz se crea una nota persistente en `40_MEMORY`.
